[pull]
	rebase = false
[user]
	email = {{ .email | quote }}
	name = Karim Khaleel
[core]
	editor = nvim
[init]
	defaultBranch = main
{{- if and (eq .chezmoi.hostname "DESKTOP-LR9MA04") (eq .chezmoi.os "windows") }}
[safe]
    directory = C:/Users/Khaleel/.local/share/chezmoi
{{- end}}